import styles from './styles.module.css';

const Menu = () => (
  <nav className={styles.nav}>
    <ul className={styles.ul}>
      <li>Home</li>
      <li>About</li>
    </ul>
  </nav>
);

export default Menu;
